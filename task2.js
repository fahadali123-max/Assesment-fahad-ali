const numarr = [1, 2, 3, 3, 3, 4, 5, 6, 3, 7, 7, 8, 9, 0, 0, 7, 6]
let arr = new Set(numarr)
console.log(arr);
// Set() is a method of array. If an array has so many duplicate data then this method is used to prevent duplication and dispaly the data without duplicating the same data. It display the data once that is duplicating.


